apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: postgres-ai-data
  name: postgres-ai-data
  namespace: studyhub-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Mi
