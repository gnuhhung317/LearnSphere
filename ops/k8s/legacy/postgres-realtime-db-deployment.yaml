apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: postgres-realtime-db
  name: postgres-realtime-db
  namespace: studyhub-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: postgres-realtime-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: postgres-realtime-db
    spec:
      containers:
        - env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_DB
              value: realtime_service_db
            - name: POSTGRES_PASSWORD
              value: password
            - name: POSTGRES_USER
              value: user
          image: postgres:16-alpine
          name: postgres-realtime-db
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-realtime-data
      restartPolicy: Always
      volumes:
        - name: postgres-realtime-data
          persistentVolumeClaim:
            claimName: postgres-realtime-data
