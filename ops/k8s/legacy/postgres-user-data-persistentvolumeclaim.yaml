apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: postgres-user-data
  name: postgres-user-data
  namespace: studyhub-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Mi
