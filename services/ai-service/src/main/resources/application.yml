server:
  port: 8086

spring:
  application:
    name: ai-service
  profiles:
    active: dev

---
spring:
  config:
    activate:
      on-profile: dev
  kafka:
    bootstrap-servers: localhost:9094
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_service_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  ai:
    chat:
      client:
        enabled: true
    google:
      genai:
        chat:
          options:
            model: gemini-2.5-flash-lite
            #thinking-level: LOW
        api-key: AIzaSyCYM42dx8TR_1Nook5G96Ra3P70xSs3ToQ
        embedding:
          api-key: AIzaSyCYM42dx8TR_1Nook5G96Ra3P70xSs3ToQ
        # project-id: studyhub-ai
        # location: us-central1
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.studyhub: DEBUG

eureka:
  client:
    service-url:
      defaultZone:  http://eureka:eureka@localhost:8761/eureka/
